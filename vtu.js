function greet(){
  confirm("submit");
}

function random()
    {
    var a=document.getElementById("state").value;
    if(a=="Goa")
    {
      var arr[]={'goa','panji','neeru'};

    }
    for(var i=0;i<arr.length();i++)
    {
      document.getElementById("district")=arr[i];
    }
    }
